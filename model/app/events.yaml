file_format: definition/2
events:
  - name: app.nav_complete
    requirement_level: recommended
    stability: development
    brief: >
      Fired when an app navigation event has completed.
    attributes:
      - ref: app.nav.destination
        requirement_level: recommended
      - ref: session.id
        requirement_level: recommended
